
export const resetCounter = (counter) => {

    if (counter == isNaN) {   // if counter is not a number shows the error
        throw new Error('We could not find the element, please recharge the page');
    }
    else {
        return counter = 0;
    }

}